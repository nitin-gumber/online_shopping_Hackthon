apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: online-shop-shop
  namespace: online-shop

spec:
  rules:
  - http:
      paths:
      - path: /shop
        pathType: Prefix
        backend:
          service:
            name: online-shop-svc
            port:
              number: 5173
